'use strict'; // eslint-disable-line strict

function position(mixin, position, top, right, bottom, left) {
  return {
    position,
    top,
    right,
    bottom,
    left
  };
}


module.exports = {
  position
};
