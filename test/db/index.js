/*import mockery from 'mockery';
import sinon from 'sinon';
import chai from 'chai';
chai.should();
*/

describe('db', function() {
  it('tests connect should connect to the DB');
  it('tests initialise creates the correct collections');
  it('tests initialise resolves with the given db');
  it('tests getDB called connect and initialise');
});